var index = require("./index2");
var counter = require("./counter");

function add(a,b) {
    return a + b + y + z + 66;
}
var x = 10;
var y = 100;
var z = 50;

m = 100; // 模块内定义的变量要使用var  否则为全局变量
// console.log(add(1,2));
//定义模块 需要对外留接口
//将模块导出 并设置导出接口
//冒号前是对外的接口 冒号后是真正的接口
module.exports = {
    dd: add,
    y : x,
    index : index,
    counter : counter
};